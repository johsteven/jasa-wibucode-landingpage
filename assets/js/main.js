// Variables

// Functions

// Main
window.addEventListener("load", init);

function init() {
    load();
    eventListener();
}

function load() {
    AOS.init({
        once: true
    });
}

function eventListener() {}